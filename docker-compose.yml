services:
  frontend:
    build:
      context: pet_shop_client
      dockerfile: Dockerfile
    ports:
     - "5173:5173"
    depends_on:
      - backend
   

  backend:
    build:
      context: pet-shop-server
      dockerfile: Dockerfile
    ports:
      - "3333:3333"
    environment:
      PORT: 3333